language: python
python:
    - "2.7"
    - "3.2"
    - "3.3"
    - "3.4"
    - "3.5"
    - "3.6"
sudo: false

install:
    - source activate test-environment
    - pip install coveralls
    - "python setup.py install"

# Command to run tests not sorted yet.  Will be in section starting "script:"

after_success:
    coveralls
